#!/usr/bin/env python3
"""luma-catalog-curator — entrypoint, runnable straight from a checkout."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))
from curator.cli import main  # noqa: E402

raise SystemExit(main())
